Known Issues:

* After switching the tab sometimes holds focus. Any mouse movement will then move the tab.
* Alt+` does not work on MacOS as apparently it's a system call or something. May need to rework this. Tried, ctrl+`
  but this doesn't seem to work well either.

